# Project Context: {PROJECT_NAME}

> Generated by `/project-context` on {DATE}. Edit freely — this document is intentionally static.
> Regenerate manually with `/project-context --force` when architecture changes significantly.

## Tech Stack

| Layer | Technology | Version/Notes |
|-------|-----------|---------------|
| Language | {e.g. Python 3.14 / TypeScript 5.x / Go 1.22} | {source: pyproject.toml / package.json / go.mod} |
| Runtime | {e.g. uv / Bun / Node.js 22 / Go stdlib} | {package manager} |
| Framework | {e.g. FastAPI / Hono / Gin / None} | {if applicable} |
| Test Framework | {e.g. pytest / Jest / Vitest / Go test} | |
| Linter/Formatter | {e.g. ruff / ESLint / golangci-lint} | |
| CI | {e.g. GitHub Actions / GitLab CI / None} | |

**Package managers detected:** {list e.g. uv, bun, cargo}

## Architecture Principles

> *Why the codebase is structured the way it is — not what, but why.*

1. **{Principle Name}**: {Explanation and rationale}
2. {e.g. "Plugin isolation: Each plugin is independently distributable to avoid coupling"}
3. {e.g. "Skills over agents: Prefer skills (inline) for lightweight tasks, agents for multi-step autonomous work"}

## Module Map

> *High-level directory layout — one line per top-level module/package.*

| Module / Directory | Purpose | Key Files |
|-------------------|---------|-----------|
| {e.g. `beads-workflow/`} | {e.g. Beads issue-tracking workflow} | `agents/bead-orchestrator.md` |
| {e.g. `dev-tools/`} | {e.g. Development tooling} | `skills/*/SKILL.md` |

## Established Patterns

> *Recurring implementation patterns the team has settled on.*

### {Pattern 1 Name}
**Where**: {file/module references}
**Pattern**: {concise description}
**Why**: {rationale}

### {e.g. Skill YAML Frontmatter}
**Where**: `*/skills/*/SKILL.md`
**Pattern**: Every skill starts with `---` YAML frontmatter with `name`, `description`, `argument-hint`.
**Why**: Claude Code requires frontmatter for skill discovery and invocation.

## Critical Invariants

> *Rules that MUST hold across the entire codebase.*

1. **{Invariant name}**: {What must always be true and why}
2. {e.g. "Every skill SKILL.md must have `name:` in frontmatter — otherwise `/skill-name` invocation fails."}
3. {e.g. "Python scripts with external deps must use PEP 723 inline metadata"}

## Enforcement Matrix

> Generated by `/project-context`. Shows Trinity coverage per contract × package.
> Legend: ✅ present · ⚠️ partial · ❌ missing · n/a not-applicable
> Sub-columns per cell: ADR | Helper | Proactive | Reactive

{Insert table output from enforcement_matrix_scanner.py here, or "No contracts declared yet" if no ADRs exist}

---
*Generated by `/project-context` · Edit freely · Regenerate with `/project-context --force`*
