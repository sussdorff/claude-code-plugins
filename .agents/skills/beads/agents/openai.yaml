interface:
  display_name: "Beads"
  short_description: "Dispatch bead implementation to the bead-orchestrator (full, Phase 0–16 end-to-end including session-close) or quick-fix agent (lightweight, Phase 0–5 end-to-end including session-close). Use when implementing a bead by ID, or to list ready beads. Triggers on implementiere bead, arbeite an bead, slice bead, bead zu gross."
  default_prompt: "Use the beads skill to help with this task. Dispatch bead implementation to the bead-orchestrator (full, Phase 0–16 end-to-end including session-close) or quick-fix agent (lightweight, Phase 0–5 end-to-end including session-close). Use when implementing a bead by ID, or to list ready beads. Triggers on implementiere bead, arbeite an bead, slice bead, bead zu gross."
